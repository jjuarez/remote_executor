#!/usr/bin/env ruby

$:.unshift File.join( File.dirname( __FILE__ ), %w[.. lib] )

require 'rubygems'
require 'remote_executor'

##
# ::Main::
Cli::Application.run( Choice.choices[:system], Choice.choices[:command] )